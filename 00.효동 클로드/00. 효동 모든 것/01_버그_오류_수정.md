---
description: 전체 프로그램 버그/오류 수정 및 개선 이력 (60건)
updated: 2026-03-25
---

# 🐛 버그/오류 수정 이력

> 모든 프로그램의 버그 수정, 오류 해결, 개선 사항을 누적 기록합니다.
> ✅ 해결 완료 / ⚠️ 알려진 한계·미해결

<br>

---

## 📋 발행 검수 프로그램 (`발행검수\main.py`) — 18건

### ✅ 해결된 버그

| # | 버전 | 날짜 | 문제 | 수정 내용 |
|:-:|:----:|:----:|------|-----------|
| 1 | v1.4 | 03-20 | 위젯/이미지 링크 분류가 **반대로** 표시 — oglink 위젯 내부 썸네일 `<img>` 때문에 위젯→이미지, 이미지→위젯 오분류 | `_add_link()`에서 widget 확인 시 image_links에서 제거, widget을 image보다 우선 체크 |
| 2 | v1.5 | 03-20 | 링크 검수 전반 부정확 — 리다이렉트 URL 비교 한계, 상품/광고 같은 MKT ID 시 광고 미매칭, 모바일 URL 미인식 | 전면 개편: 3단계 매칭(전체URL→MKT ID→리다이렉트), `if/elif`→독립 `if/if`, regex `(?:m\.)?` 추가 |
| 3 | v1.6 | 03-20 | 광고 링크 `_bc05` 파라미터 소실로 "광고 링크 미삽입" 오탐 + 배너 2~3건 중복 수집 | `data-linkdata` 속성에서 원본 MKT URL 우선 추출, `processedBanners`로 oglink 단위 중복 제거 |
| 4 | v1.7 | 03-23 | 임시 DOCX 다운로드 파일이 `Temp\`에 계속 쌓임 (리소스 누수) | `_cleanup_tmp()` 메서드로 검수 완료/중단 시 일괄 삭제 |
| 5 | v1.7 | 03-23 | EXE 실행 시 `credentials.json` 경로 못 찾음 | `sys.frozen` 분기로 EXE 옆 파일 우선 → 기존 경로 fallback |
| 6 | v1.8 | 03-25 | `★블로거 요청사항★` 이후 `---` 없는 원고에서 **본문 전체가 instructions로 잡혀** 200건+ 오탐 | `in_req` 모드에서 지시사항 특성 줄만 캡처, 아니면 즉시 `in_req=False` 전환 |
| 7 | v1.9 | 03-25 | 검수 결과 중복 보고 — instructions 중복 + 서식키워드↔요청문구 겹침 | `req_seen` set + `req_exposed` 리스트로 중복 방지 |
| 8 | v1.5 | 03-20 | `page_source` regex 방식 부정확 | page_source 폴백 제거, 실제 `<a>` 태그와 위젯에서만 수집 |
| 9 | v1.1 | 03-19 | 블로그 링크 수집 누락 — "동일한 것만 두 개" | v1.5 JS 기반 전체 수집 + v1.6 data-linkdata로 해결 |

### ⚠️ 미해결 / 알려진 한계

| # | 문제 | 상태 |
|:-:|------|------|
| 10 | DOCX 다운로드 실패 빈번 — `find_and_download()` 파일명 불일치/권한 | 부분 개선, 근본 미해결 |
| 11 | 서식 키워드 오탐 — "글씨 크기가 작아서" 같은 본문도 잡힘 | 알려진 한계 |
| 12 | 이미지 번호 "0" 오탐 — 본문의 자연스러운 "0"도 잡힘 | 알려진 한계 |
| 13 | 서식 매칭 첫 20자 기준 오매칭 — 짧은/중복 텍스트 | 알려진 한계 |
| 14 | 형광펜(highlight) 검증 미구현 | 미구현 |
| 15 | 인용구 1~6번 구분 없이 `in_quote` 여부만 체크 | 미구현 |
| 16 | URL 리졸브 속도 — 매번 HTTP 요청, 글로벌 캐시 없음 | 미해결 |
| 17 | 스크래핑 실패 시 재시도 로직 없음 — 빈 결과 반환 | 미해결 |
| 18 | 드라이버 좀비 프로세스 — 예외 시 Chrome 미종료 | 미해결 |

<br>

---

## 💬 댓글 알림 프로그램 (`댓글알림\main.py`) — 8건

### ✅ 해결된 버그

| # | 버전 | 날짜 | 문제 | 수정 내용 |
|:-:|:----:|:----:|------|-----------|
| 19 | v2.0 | 03-23 | Chrome 탭 크래시 빈번 | `page_load_timeout(15)`, 10개마다 드라이버 재시작, `--single-process` 제거 |
| 20 | v2.1 | 03-23 | `PostTitleListAsync` API에서 일부 글 누락 → 댓글 변동 감지 불가 | 모바일 페이지에서 `commentCount` 직접 추출로 전환 |
| 21 | v2.1 | 03-23 | cbox API `"Wrong ticket"` 에러 | cbox 포기, 모바일 페이지 방식으로 전환 |
| 22 | v2.2 | 03-23 | 비공개 글에서 `commentCount` 없음 → 새 댓글 놓침 | Selenium 직접 댓글 확인 fallback 추가 |
| 23 | v2.0 | 03-23 | v1.0 전체 Selenium 스크래핑 100분+ 소요 | 2단계 최적화 (API 감지→Selenium) → 8초 |
| 24 | - | 03-23 | `find_elements` vs `find_element` — implicit wait 병목 | `find_elements` 사용으로 전환 |
| 25 | - | 03-23 | 삭제된 게시글 접속 시 alert 미처리 → Selenium 에러 | `switch_to.alert.accept()` 처리 추가 |

### ⚠️ 미해결 / 알려진 한계

| # | 문제 | 상태 |
|:-:|------|------|
| 26 | `config.json` 기존 값이 코드 기본값보다 우선 — 코드 수정해도 반영 안 됨 | 알려진 동작 |

<br>

---

## 📝 원고 배정 프로그램 (`원고배정\main.py`) — 8건

### ✅ 해결된 버그

| # | 날짜 | 문제 | 수정 내용 |
|:-:|:----:|------|-----------|
| 27 | 03-24 | 원고 본문 큰 숫자(2024, 100)를 이미지 번호로 **오인식** | `int(text) <= 30` 조건 추가 |
| 28 | 03-20 | 제외 키워드 설정 버튼을 Step 3에만 넣었더니 **못 찾음** | 상단 바로 이동, 어느 단계에서든 접근 가능 |
| 29 | 03-24 | Step 4 시트 기입이 `if param_updates:` 조건에 막혀 키워드 시트 기입 스킵 | 조건 제거 → 항상 `_auto_update_sheets()` 호출 |
| 30 | 03-20 | Step 1 기존 데이터 경고 시 발행리스트 불러오기 미실행 | `askyesno` → "예" 시 불러오기 실행으로 개선 |

### ⚠️ 미해결 / 알려진 한계

| # | 문제 | 상태 |
|:-:|------|------|
| 31 | 시트 기입 취소 후 Step 1 Treeview 미갱신 (새로고침 필요) | 알려진 한계 |
| 32 | 수식 반영 대기 시간 고정 3초 — 시트 부하 시 불충분 가능 | 알려진 한계 |
| 33 | `manual_pass.json` 항목 계속 쌓임 (자동 정리 없음) | 알려진 한계 |
| 34 | 제외 키워드 부분 매칭만 — "탈모" 등록 시 "탈모예방"도 걸림 | 알려진 한계 |

<br>

---

## ✍️ 원고 제작기 (`manuscript_generator\main.py`) — 7건

### ✅ 해결된 버그

| # | 날짜 | 문제 | 수정 내용 |
|:-:|:----:|------|-----------|
| 35 | 03-19 | `VALID_FONT_SIZES`에 21 포함 → 글자 크기 20 문제 | 21 제거 + 프롬프트 제약 추가 |

### ⚠️ 미해결 / 알려진 한계

| # | 문제 | 상태 |
|:-:|------|------|
| 36 | `main.py` 3,482줄 단일 파일 → 유지보수 어려움 | 알려진 한계 |
| 37 | API 키 `.api_key` 파일에 평문 저장 | 보안 문제 |
| 38 | 모델명 `claude-sonnet-4-20250514` 하드코딩 | 알려진 한계 |
| 39 | ㄴ서식 파싱 — regex+문자열 혼합, 디버깅 어려움, 실패 시 사용자 피드백 없음 | 알려진 한계 |
| 40 | 테스트 코드 없음 — 프롬프트 조립/서식 파싱 검증 부재 | 미구현 |
| 41 | 창 크기/레이아웃 저장 안 됨 — 매번 리셋 | 미구현 |

<br>

---

## 🔗 블로그 MKT 링크 대조 (`blog_link_extractor.py`) — 6건

### ✅ 해결된 버그

| # | 날짜 | 문제 | 수정 내용 |
|:-:|:----:|------|-----------|
| 42 | 03-20 | MKT 링크 2개인데 1개만 수집 — base URL 중복 제거가 원인 | 중복 제거 로직 제거 |

### ⚠️ 미해결 / 알려진 한계

| # | 문제 | 상태 |
|:-:|------|------|
| 43 | 34개 중복 수집 — DOM/data-linkdata/소스 3방식이 같은 링크 반복 | 미해결 |
| 44 | `ws.update()` DeprecationWarning (gspread 인자 순서) | 미해결 |
| 45 | 순차 처리 120개 블로그 ~10분 — 병렬화 필요 | 미해결 |
| 46 | 시트/탭명 `시트34` 하드코딩 | 미해결 |
| 47 | MKT 링크 3개 이상 시 H/J열만 기록 → 3번째+ 누락 | 미해결 |

<br>

---

## 💬 댓글 검수 프로그램 (`댓글검수\main.py`) — 5건

### ✅ 해결된 버그

| # | 날짜 | 문제 | 수정 내용 |
|:-:|:----:|------|-----------|
| 48 | 03-20 | v1.0 순차 처리 느림 | v2.1 병렬 스크래핑(4개 동시) → ~4배 속도 |
| 49 | 03-20 | E열 초기화 안 됨 — 이전 미등록 댓글 잔존 | v2.1 `batch_clear` 후 새 결과 기록 |
| 50 | 03-20 | 탭명 `시트23` 하드코딩 | v2.1 `댓글 비교`로 변경 |

### ⚠️ 미해결 / 알려진 한계

| # | 문제 | 상태 |
|:-:|------|------|
| 51 | 대댓글 구분 미구현 — 댓글과 대댓글 혼합 수집 | 미구현 |
| 52 | 브라우저 풀 미재사용 — URL마다 생성/종료 | 알려진 한계 |

<br>

---

## 📊 매출 업로더 (`sales_uploader.py`) — 5건

### ✅ 해결된 버그

| # | 날짜 | 문제 | 수정 내용 |
|:-:|:----:|------|-----------|
| 53 | 03-19 | `append_rows` 사용으로 T+ 컬럼 데이터 위치 불일치 | `ws.update()` + 명시적 범위 지정으로 교체 |

### ⚠️ 미해결 / 알려진 한계

| # | 문제 | 상태 |
|:-:|------|------|
| 54 | API/Excel 에러 시 예외 처리 없음 — 크래시, 부분 업로드 롤백 없음 | 미해결 |
| 55 | 업로드 후 검증 없음 — 실제 기록 데이터 대조 미구현 | 미해결 |
| 56 | 중복 실행 방지 없음 — 같은 파일 여러 번 업로드 가능 | 미해결 |
| 57 | 파일명에 날짜 없으면 `sys.exit(1)` 크래시 | 알려진 한계 |

<br>

---

## 🖼️ 이미지 원고 제작기 — 2건

### ✅ 해결된 버그

| # | 날짜 | 문제 | 수정 내용 |
|:-:|:----:|------|-----------|
| 58 | 03-23 | Haiku Vision 정확도 매우 낮음 — 모자이크 이미지도 추측으로 채움 | Sonnet 4 + 맥락 프롬프트(폴더/파일명)로 전환 |
| 59 | 03-23 | Claude 3 Haiku Vision 미지원 (404 에러) | Sonnet 4로 최종 결정 |

<br>

---

## 🔄 기타 — 1건

| # | 문제 | 상태 |
|:-:|------|------|
| 60 | `claude --continue` 세션 만료로 복구 불가 (두 번 발생) | 외부 도구 한계, 메모리 상세 저장으로 대응 |

<br>

---

## 📈 통계 요약

| 구분 | 건수 |
|------|:----:|
| ✅ 해결 완료 | **35건** |
| ⚠️ 미해결/알려진 한계 | **25건** |
| **합계** | **60건** |

<br>

## 🔍 공통 교훈 & 반복 패턴

| 패턴 | 설명 | 사례 |
|------|------|------|
| **네이버 JS 간섭** | DOM/링크를 JS로 동적 수정 → `a.href`가 원본과 다름 | 발행 검수 v1.6 |
| **API 누락/제한** | 네이버 API가 일부 데이터 미반환 또는 에러 | 댓글 알림 v2.1 |
| **오탐(false positive)** | 검수 로직이 넓게 잡아 정상→오류 판정 | 발행 검수 v1.8 |
| **중복 보고** | 같은 문제를 여러 경로에서 중복 감지 | 발행 검수 v1.9 |
| **리소스 누수** | 임시 파일/Chrome 프로세스 미정리 | 발행 검수 v1.7, 댓글 알림 v2.0 |
| **하드코딩** | 시트명/탭명/모델명 코드에 고정 | MKT 대조, 댓글 검수, 원고 제작기 |
| **예외 처리 부재** | 에러 시 크래시, 롤백/재시도 없음 | 매출 업로더, 발행 검수 |
